class Transaction {
  String id;
  String name;
  double amount;
  DateTime time;

  Transaction({
    required this.id,
    required this.name,
    required this.amount,
    required this.time,
  });
}
